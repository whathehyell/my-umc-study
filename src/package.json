{
  "name": "src",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon -e js,json,prisma --exec \"npx prisma generate --schema=../prisma/schema.prisma && node index.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0",
    "nodemon": "^3.1.0",
    "prisma": "^6.18.0",
    "@prisma/client": "^6.18.0"
  }
}

